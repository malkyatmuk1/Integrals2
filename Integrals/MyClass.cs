using System;
using secondIntegrals.Integrals;
namespace secondIntegrals
{
	public class MyClass
	{
		public static void Main(string[] args)
		{
			int a,b,n,r,eps,t,x;
			double h;
			Integrals integ=new Integrals(b,a,n,r,eps);

		}
	}
}
